# Frontend Telemetry Package

Planned browser-safe helpers for trace propagation, sanitized client telemetry, performance signals, and correlation identifiers shared by MatchMate frontend applications.

This package must not contain domain logic, backend-only telemetry adapters, secrets, or sensitive user data.
